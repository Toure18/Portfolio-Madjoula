{% block stylesheets %}
    {{ encore_entry_link_tags('nav') }}
{% endblock %}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid d-flex flex-column justify-content-center">
        <img src="{{ asset('build/IMG_0058.jpg') }}" alt="photo-profil" class="rounded-circle">
        <a class="navbar-brand text-light text-bold display-1" href="{{ path('home') }}">Portfolio'Madjoula</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon fs-5"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="{{ path('course') }}">Mon parcours</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('formation') }}">Mes formations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('documents') }}">Mes documents</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                </li>
            </ul>
        </div>
    </div>
</nav>